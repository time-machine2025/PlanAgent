# data directory

Runtime data is generated here by `scheduler_agent.py init`.

This folder is intentionally kept out of Git tracking (except this file and `.gitkeep`) to avoid uploading personal planning data.
